# COLLAPSE Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gộp đủ 4 thẻ của một nhóm CÓ nhóm cha thì sinh ra 1 thẻ mới (mang mặt nhóm vừa gộp, thuộc nhóm cha) đặt vào chính hộp đó — hộp không bị xoá, hộp dưới không lộ ra.

**Architecture:** Toàn bộ luật nằm trong `PrototypeDomain.cs` (C# thuần, không UnityEngine) để `./selfcheck.sh` phủ được; view chỉ thêm một nhánh animation trong `BoardController.Settle()`. Spec đã chốt mọi quyết định: `docs/superpowers/specs/2026-08-06-collapse-design.md` — đọc nó TRƯỚC khi làm task nào.

**Tech Stack:** C# (Unity 6.3, nhưng domain build bằng csc qua `selfcheck.sh`), DOTween cho view.

## Global Constraints

- **Làm việc tại `D:\CategorySort` (repo chính)** — KHÔNG phải worktree `.claude/worktrees/...`. Unity đang mở ở repo chính.
- `PrototypeDomain.cs` **cấm import UnityEngine** — mất ràng buộc là mất `./selfcheck.sh`.
- Vòng test = `./selfcheck.sh` (domain, ~5s, không cần Unity). Đụng `BoardController.cs` thì thêm `./compilecheck.sh`.
- C# kiểu cũ theo file: `out int n` khai trước dòng gọi, không `?.`, không string interpolation, comment **tiếng Việt**, indent 4 space.
- Working tree đang có file dở của user (Tile.prefab, Assets/Sprites/, ShaderGraph/...) — **`git add` đích danh từng file, cấm `git add -A` / `git add .`**.
- Commit message tiếng Anh, dòng đầu mệnh lệnh ngắn, kết bằng `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- File `.md` của repo có thể là CRLF — normalize trước khi Edit khớp chuỗi.
- Số nhóm thành viên: **member của nhóm = card khai trực tiếp + nhóm con trỏ vào nó = đúng `Rules.GroupSize` (4)**, áp cho MỌI nhóm kể cả gốc.

---

### Task 1: Validate mở cửa COLLAPSE

**Files:**
- Modify: `Assets/Prototype/PrototypeDomain.cs:306-322` (vòng foreach group đầu tiên trong `Validate`)
- Modify: `Assets/Prototype/PrototypeDomain.cs:768` (case SelfCheck `"group có cha (COLLAPSE)"`)

**Interfaces:**
- Produces: `Validate` chấp nhận `GroupDef.ParentId != null` khi: cha tồn tại, không chu trình, member count = 4, có ≥1 nhóm gốc. Task 2-4 dựa vào việc level cha-con parse + validate sạch.
- Produces: hằng `SelfCheck.CollapseLv` (json mini 2 nhóm leaf→root) — mọi task sau dùng nó làm fixture.

- [ ] **Step 1: Viết test fail — thêm block case COLLAPSE vào SelfCheck**

Trong `SelfCheck.Run`, ngay SAU dòng case `broken(l => l.Groups[0].ParentId = l.Groups[1].Id, "group có cha (COLLAPSE)");` — XOÁ dòng đó (hành vi nó kiểm sắp thành hợp lệ) và chèn:

```csharp
            // ---- 1b. COLLAPSE: level cha-con hợp lệ phải pass, level hỏng phải chết ----
            {
                Func<LevelData> freshC = () => LevelData.Parse(CollapseLv);
                Action<Action<LevelData>, string> brokenC = (mutate, label) =>
                {
                    var lv = freshC();
                    mutate(lv);
                    bool threw = false;
                    try { lv.Validate(hasArt); } catch { threw = true; }
                    Ok(threw, "validate phải ném lỗi: " + label);
                };

                freshC().Validate(hasArt);   // hợp lệ: leaf (4 card, cha root) + root (3 card + 1 con)

                brokenC(l => l.Groups[0].ParentId = "ghost", "nhóm cha không tồn tại");
                brokenC(l => l.Groups[0].ParentId = "leaf", "nhóm tự làm cha chính nó");
                brokenC(l => l.Groups[1].ParentId = "leaf", "chu trình leaf ↔ root (và hết nhóm gốc)");
                brokenC(l =>
                {
                    l.Groups[0].Cards.Add(new CardDef { Id = "l5", Text = "L5" });
                    l.Stacks[1].Boxes[0].Slots[3] = "l5";
                }, "nhóm 4 card + 1 con = 5 thành viên");
            }
```

Và thêm hằng json vào đầu `SelfCheck` (ngay sau `static void Ok(...)`):

```csharp
        // Level mini cho COLLAPSE: leaf (4 thẻ, cha = root) đủ bộ NGAY trong hộp trên của
        // stack 0 → nổ ngay nhịp Settle đầu tiên. root (3 thẻ + 1 con) nằm ở stack 1.
        // Stack 0 có hộp đáy rỗng bên dưới để test luật xoá hộp sau collapse ở chế độ chặt.
        const string CollapseLv = @"{
          ""id"":""t-collapse"", ""title"":""t"", ""note"":"""",
          ""layout"": { ""stacks"": [
            { ""pos"":[0,0], ""boxes"":[ { ""slots"":[""l1"",""l2"",""l3"",""l4""] },
                                          { ""slots"":[null,null,null,null] } ] },
            { ""pos"":[1,0], ""boxes"":[ { ""slots"":[""r1"",""r2"",""r3"",null] } ] }
          ]},
          ""meaning"": { ""groups"": [
            { ""id"":""leaf"", ""text"":""Leaf"", ""group"":""root"", ""cards"":[
              { ""id"":""l1"",""text"":""L1"" },{ ""id"":""l2"",""text"":""L2"" },
              { ""id"":""l3"",""text"":""L3"" },{ ""id"":""l4"",""art"":""apple"" } ]},
            { ""id"":""root"", ""text"":""Root"", ""cards"":[
              { ""id"":""r1"",""text"":""R1"" },{ ""id"":""r2"",""text"":""R2"" },
              { ""id"":""r3"",""text"":""R3"" } ]}
          ]}
        }";
```

- [ ] **Step 2: Chạy để thấy fail**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SELFCHECK FAIL: [t-collapse] group "leaf" có nhóm cha → COLLAPSE, phạm vi này chỉ hỗ trợ CLEAR` (dòng positive `freshC().Validate` chết vì Validate còn chặn ParentId).

- [ ] **Step 3: Sửa `Validate`**

Trong vòng `foreach (var g in Groups)` đầu tiên, **xoá** 2 khối này:

```csharp
                if (g.ParentId != null)
                    die("group \"" + g.Id + "\" có nhóm cha → COLLAPSE, phạm vi này chỉ hỗ trợ CLEAR");
                if (g.Cards.Count != Rules.GroupSize)
                    die("group \"" + g.Id + "\" có " + g.Cards.Count + " thẻ, phải đúng " + Rules.GroupSize);
```

Ngay SAU vòng foreach đó (trước vòng `foreach (var g in Groups) foreach (var c in g.Cards)`), chèn block:

```csharp
            // -- COLLAPSE: quan hệ nhóm cha-con --
            // Member của một nhóm = card khai trực tiếp + nhóm con trỏ vào nó. Nhóm nào
            // (kể cả gốc) cũng phải đủ đúng GroupSize thành viên thì mới gộp được.
            var childCount = new Dictionary<string, int>();
            bool hasRoot = false;
            foreach (var g in Groups)
            {
                if (g.ParentId == null) { hasRoot = true; continue; }
                if (g.ParentId == g.Id) die("group \"" + g.Id + "\" tự làm cha chính nó");
                if (!gids.Contains(g.ParentId))
                    die("group \"" + g.Id + "\" trỏ nhóm cha \"" + g.ParentId + "\" không tồn tại");
                int n;
                childCount.TryGetValue(g.ParentId, out n);
                childCount[g.ParentId] = n + 1;
            }
            if (!hasRoot)
                die("phải có ít nhất một nhóm gốc (không có \"group\") — không thì không thẻ nào biến mất được");

            // Chu trình: đi theo cha; quá số nhóm là chắc chắn có vòng.
            var byId = Groups.ToDictionary(x => x.Id);
            foreach (var g in Groups)
            {
                var cur = g;
                int steps = 0;
                while (cur.ParentId != null)
                {
                    if (++steps > Groups.Count)
                        die("chuỗi nhóm cha-con có chu trình (đi từ \"" + g.Id + "\")");
                    cur = byId[cur.ParentId];
                }
            }

            foreach (var g in Groups)
            {
                int cn;
                childCount.TryGetValue(g.Id, out cn);
                if (g.Cards.Count + cn != Rules.GroupSize)
                    die("group \"" + g.Id + "\" có " + g.Cards.Count + " thẻ + " + cn +
                        " nhóm con = " + (g.Cards.Count + cn) + " thành viên, phải đúng " + Rules.GroupSize);
            }
```

Cập nhật luôn comment cuối `Validate` (dòng ~374): xoá 2 dòng "Chưa cần kiểm cycle... Q3" vì đã kiểm.

- [ ] **Step 4: Chạy pass**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SelfCheck OK — 4 level, luật khớp demo/check.mjs` (4 case brokenC đều threw, positive pass, 4 level cũ vẫn sạch — mọi nhóm cũ 4 card + 0 con = 4).

- [ ] **Step 5: Commit**

```bash
cd "D:/CategorySort" && git add Assets/Prototype/PrototypeDomain.cs && git commit -m "$(cat <<'EOF'
Open Validate to parent groups, with the four COLLAPSE rules

Member count is now cards + child groups == GroupSize for every group,
parent references must resolve, chains must be acyclic, and at least one
root group must exist. The old hard block on ParentId is gone; the
SelfCheck fixture CollapseLv (leaf -> root) starts its life here.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `Game` giữ bảng nhóm, `Box` nhớ đã collapse

**Files:**
- Modify: `Assets/Prototype/PrototypeDomain.cs:395-405` (`Box`), `:430-470` (`Game` + `Build`), `:601-611` (`Clone`)

**Interfaces:**
- Consumes: `CollapseLv` từ Task 1.
- Produces: `public Dictionary<string, GroupDef> GroupDefs` trên `Game` (key = group id; **Clone chia sẻ tham chiếu**, không sao sâu). `public bool HadCollapse` trên `Box` (Clone sao chép). Task 3-4 đọc cả hai.

- [ ] **Step 1: Viết test fail — thêm vào CUỐI block `1b. COLLAPSE` của Task 1**

```csharp
                var lvB = freshC();
                lvB.Validate(hasArt);
                var gB = Game.Build(lvB);
                Ok(gB.GroupDefs != null && gB.GroupDefs["leaf"].ParentId == "root"
                   && gB.GroupDefs["root"].ParentId == null,
                   "Game phải mang bảng nhóm gid → def");
                Ok(ReferenceEquals(gB.GroupDefs, gB.Clone().GroupDefs),
                   "Clone chia sẻ bảng nhóm (chỉ-đọc), không sao sâu");
                var boxB = new Box { HadCollapse = true };
                Ok(boxB.Clone().HadCollapse, "Box.Clone phải chép cờ HadCollapse");
```

- [ ] **Step 2: Chạy fail**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: compile FAIL — `'Game' does not contain a definition for 'GroupDefs'` (selfcheck.sh build bằng csc nên lỗi compile hiện ở đây).

- [ ] **Step 3: Implement**

`Box` — thêm field + chép trong Clone:

```csharp
    public class Box
    {
        public bool IsBottom;
        public bool HadCollapse;   // đã từng xảy ra collapse — chế độ chặt dùng để xoá hộp rỗng
        public Tile[] Slots = new Tile[Rules.BoxCapacity];
        public Box Clone()
        {
            var b = new Box { IsBottom = IsBottom, HadCollapse = HadCollapse, Slots = new Tile[Slots.Length] };
            for (int i = 0; i < Slots.Length; i++) b.Slots[i] = Slots[i] == null ? null : Slots[i].Clone();
            return b;
        }
    }
```

`Game` — thêm field ngay dưới `public List<Stack> Stacks...`:

```csharp
        // gid → định nghĩa nhóm (ParentId/Text/Art) — SettleStep tra khi gộp. Chỉ-đọc sau
        // Build nên Clone chia sẻ tham chiếu: solver clone hàng vạn lần, sao sâu là phí thật.
        public Dictionary<string, GroupDef> GroupDefs;
```

Trong `Build`, sau dòng `var g = new Game { ... };` thêm:

```csharp
            g.GroupDefs = lv.Groups.ToDictionary(x => x.Id);
```

Trong `Clone`, thêm `GroupDefs = GroupDefs,` vào object initializer (cạnh `uidSeq = uidSeq`).

- [ ] **Step 4: Chạy pass**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SelfCheck OK — 4 level, ...`

- [ ] **Step 5: Commit**

```bash
cd "D:/CategorySort" && git add Assets/Prototype/PrototypeDomain.cs && git commit -m "$(cat <<'EOF'
Carry the group table on Game and a HadCollapse flag on Box

Build used to bake group data into tiles and throw the level away, so
SettleStep had no way to ask who a group's parent is. Clone shares the
table by reference -- it is read-only and the solver clones per node.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: `SettleStep` rẽ nhánh collapse

**Files:**
- Modify: `Assets/Prototype/PrototypeDomain.cs:419-428` (`SettleKind`, `SettleEvent`), `:514-538` (nhánh clear của `SettleStep`)

**Interfaces:**
- Consumes: `GroupDefs`, `HadCollapse` (Task 2).
- Produces: `SettleKind.Collapse`; `SettleEvent.NewTileUid` (string, uid thẻ vừa sinh). Thẻ sinh ra: `Uid` mới từ `uidSeq`, `CardId = <gid nhóm gộp>`, `GroupId = <ParentId>`, `Text/Art = <của nhóm gộp>`, đặt vào **ô trống đầu tiên**. `Cleared++` cả hai nhánh. Task 6 (view) đọc `NewTileUid`.

- [ ] **Step 1: Viết test fail — nối tiếp block 1b**

```csharp
                // Collapse nổ ngay nhịp Settle đầu: 4 thẻ leaf cùng hộp → 1 thẻ "Leaf"
                var gC = Game.Build(lvB).Settle(false);
                var topC = gC.TopBox(0);
                Ok(gC.Stacks[0].Boxes.Count == 2, "collapse KHÔNG xoá hộp, hộp dưới KHÔNG lộ");
                Ok(topC.HadCollapse, "hộp phải ghi nhớ đã collapse");
                Ok(topC.Slots[0] != null && topC.Slots[0].CardId == "leaf"
                   && topC.Slots[0].GroupId == "root" && topC.Slots[0].Text == "Leaf"
                   && topC.Slots[0].Art == null,
                   "thẻ sinh ra mang mặt nhóm leaf, thuộc nhóm root, ở ô trống đầu tiên");
                Ok(topC.Slots[1] == null && topC.Slots[2] == null && topC.Slots[3] == null,
                   "ba ô còn lại phải trống");
                Ok(gC.Cleared == 1, "collapse tính là một lần clear");
```

- [ ] **Step 2: Chạy fail**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SELFCHECK FAIL: collapse KHÔNG xoá hộp...` — hành vi hiện tại CLEAR 4 thẻ leaf rồi xoá hộp rỗng (`Boxes.Count == 1`).

- [ ] **Step 3: Implement**

Enum + struct:

```csharp
    public enum SettleKind { None, Clear, RemoveBox, Collapse }

    public struct SettleEvent
    {
        public SettleKind Kind;
        public int Stack;
        public string GroupId;
        public string[] DoomedUids;   // uid các thẻ vừa bị xoá — view animate rồi mới Rebuild
        public string NewTileUid;     // Collapse: uid thẻ vừa sinh ra, view spawn nó
        public bool BoxRemoved;
    }
```

Trong `SettleStep`, nhánh clear — sau `Cleared++;` (giữ nguyên vòng xoá 4 thẻ vào `doomed`), chèn TRƯỚC `bool removed = ...`:

```csharp
                Cleared++;
                // Nhóm có cha → COLLAPSE: sinh 1 thẻ mang mặt nhóm vừa gộp, là thành viên
                // của nhóm cha, đặt vào ô trống đầu tiên của CHÍNH hộp này. Hộp không rỗng
                // nên luật xoá hộp bên dưới tự im — hộp dưới không lộ ra.
                // CardId = id nhóm (không phải null): Solver.Encode mã hoá bằng CardId,
                // để null là hai trạng thái khác nhau memo thành một.
                GroupDef def;
                if (GroupDefs != null && GroupDefs.TryGetValue(gid, out def) && def.ParentId != null)
                {
                    int j = Array.FindIndex(box.Slots, t => t == null);
                    var nt = new Tile
                    {
                        Uid = "t" + (++uidSeq),
                        CardId = gid, GroupId = def.ParentId, Text = def.Text, Art = def.Art
                    };
                    box.Slots[j] = nt;
                    box.HadCollapse = true;
                    return new SettleEvent
                    {
                        Kind = SettleKind.Collapse, Stack = s, GroupId = gid,
                        DoomedUids = doomed.ToArray(), NewTileUid = nt.Uid
                    };
                }
                bool removed = IsEmpty(box) && !box.IsBottom;
```

(Test cũ mục 3/4/5 của SelfCheck nhét `GroupId` giả `"zz"`, `"g0"`... không có trong `GroupDefs` → `TryGetValue` trượt → rơi xuống nhánh clear cũ, sống nguyên.)

- [ ] **Step 4: Chạy pass**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SelfCheck OK — 4 level, ...`

- [ ] **Step 5: Commit**

```bash
cd "D:/CategorySort" && git add Assets/Prototype/PrototypeDomain.cs && git commit -m "$(cat <<'EOF'
Collapse: four of a parented group become one tile of the parent

New SettleKind.Collapse event carries NewTileUid for the view. The tile
takes the group's id as CardId so Solver.Encode keeps distinguishing
states, and the box keeps its place -- nothing is revealed underneath.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Chế độ chặt học luật mới + ván thắng trọn chuỗi

**Files:**
- Modify: `Assets/Prototype/PrototypeDomain.cs:540-551` (nhánh drain của `SettleStep`), `:625-646` (`Solver.Encode`)
- Modify: `docs/superpowers/specs/2026-08-06-collapse-design.md` (Mục 6, dòng "Solver.Encode ... không phải sửa dòng nào")

**Interfaces:**
- Consumes: `HadCollapse` (Task 2), nhánh Collapse (Task 3).
- Produces: ở chế độ chặt (`drain == false`), hộp rỗng KHÔNG-đáy có `HadCollapse == true` vẫn bị xoá. `Encode` thêm dấu `!` cho box có `HadCollapse` để memo của solver không trộn hai trạng thái khác tương lai.

- [ ] **Step 1: Viết test fail — nối tiếp block 1b**

```csharp
                // Chặt: kéo thẻ sinh ra đi → hộp rỗng DO COLLAPSE → vẫn bị xoá (luật mới).
                string leafUid = topC.Slots[0].Uid;
                Ok(gC.MoveTile(0, leafUid, 1), "kéo thẻ sinh ra sang stack chứa root");
                gC.Settle(false);
                Ok(gC.Stacks[0].Boxes.Count == 1,
                   "chặt: hộp rỗng SAU COLLAPSE vẫn bị xoá, hộp đáy lộ ra");
                Ok(gC.Cleared == 2 && gC.Status == GameStatus.Won,
                   "root đủ 4 (r1+r2+r3+Leaf) → CLEAR gốc → sạch bàn → thắng");

                // Đối chứng: hộp rỗng KHÔNG do clear/collapse thì chặt giữ nguyên như cũ.
                var strictG = load(0, false);
                var t0 = strictG.TopBox(0);
                string mA = t0.Slots[0].Uid, mB = t0.Slots[1].Uid;
                int emptyS = strictG.Stacks.FindIndex(st => Game.IsEmpty(st.Boxes[0]));
                Ok(emptyS >= 0 && strictG.MoveTile(0, mA, emptyS) && strictG.MoveTile(0, mB, emptyS),
                   "dọn rỗng được hộp trên của stack 0");
                int depth0 = strictG.Stacks[0].Boxes.Count;
                strictG.Settle(false);
                Ok(strictG.Stacks[0].Boxes.Count == depth0,
                   "chặt: hộp rỗng vì bị kéo sạch thẻ thì Ở LẠI");

                // Solver giải được level collapse ở CẢ HAI chế độ, đúng 1 nước.
                foreach (bool dr in new[] { false, true })
                {
                    var lvS = freshC();
                    lvS.Validate(hasArt);
                    var rS = Solver.Solve(Game.Build(lvS).Settle(dr), dr);
                    Ok(rS.Ok && rS.Depth == 1,
                       "mini collapse phải giải được trong 1 nước, chế độ " + (dr ? "rộng" : "chặt"));
                }
```

- [ ] **Step 2: Chạy fail**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SELFCHECK FAIL: chặt: hộp rỗng SAU COLLAPSE vẫn bị xoá...` — nhánh drain hiện bọc trong `if (drain)`, chặt không xoá gì.

- [ ] **Step 3: Implement**

Nhánh drain của `SettleStep` — bỏ `if (drain)` bọc ngoài, điều kiện chuyển vào từng box:

```csharp
            // Hộp trên cùng rỗng, không phải đáy → lùi ra. Chế độ rộng (drain) xoá mọi hộp
            // rỗng; chế độ chặt chỉ xoá hộp ĐÃ TỪNG collapse — vì collapse là một lần ghép
            // bộ, chỉ khác CLEAR ở chỗ để lại một thẻ. Không có luật này thì hộp sau
            // collapse thành nắp đậy vĩnh viễn ở chế độ chặt (spec Mục 5).
            for (int s = 0; s < Stacks.Count; s++)
            {
                var box = TopBox(s);
                if (box != null && !box.IsBottom && IsEmpty(box) && (drain || box.HadCollapse))
                {
                    Stacks[s].Boxes.RemoveAt(0);
                    return new SettleEvent { Kind = SettleKind.RemoveBox, Stack = s, BoxRemoved = true };
                }
            }
```

`Solver.Encode` — hai chỗ `parts.Add(...)` thêm dấu `!`:

```csharp
                    if (i == 0)
                    {
                        var ids = b.Slots.Where(t => t != null).Select(t => t.CardId).ToList();
                        ids.Sort(StringComparer.Ordinal);
                        parts.Add((b.HadCollapse ? "!" : "") + string.Join(",", ids));
                    }
                    else parts.Add((b.HadCollapse ? "!" : "") +
                                   string.Join(",", b.Slots.Select(t => t == null ? "_" : t.CardId)));
```

Lý do (ghi thành comment ngay trên `Encode`): ở chế độ chặt, hai bàn giống hệt nhau về thẻ nhưng khác `HadCollapse` có tương lai khác nhau (một cái sắp tự mở hộp) — memo trộn chúng là solver trả kết quả sai im lặng.

Cập nhật spec `docs/superpowers/specs/2026-08-06-collapse-design.md`, Mục 6, dòng bảng `| Solver.Encode | Nhờ CardId = id nhóm, xem Mục 4 |` thành:

```markdown
| `Solver.Encode` | `CardId` = id nhóm lo phần thẻ sinh ra; nhưng phải THÊM dấu `!` cho box `HadCollapse` — chặt phân biệt hai bàn cùng thẻ khác cờ (phát hiện khi implement, 2026-08-06) |
```

- [ ] **Step 4: Chạy pass**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `SelfCheck OK — 4 level, ...` (4 level cũ không có collapse nên `!` không xuất hiện, số nước của chúng không đổi: 6/6/9/9/2/2/10/9).

- [ ] **Step 5: Commit**

```bash
cd "D:/CategorySort" && git add Assets/Prototype/PrototypeDomain.cs docs/superpowers/specs/2026-08-06-collapse-design.md && git commit -m "$(cat <<'EOF'
Strict mode removes emptied boxes that once collapsed

Without this every collapsed box becomes a permanent lid over the stack
in strict mode. Encode now marks HadCollapse boxes with '!' so the
solver's memo cannot merge two boards whose futures differ.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Đưa lv-005 / lv-006 vào game, chạy cổng xuất bản

**Files:**
- Move: `docs/levels-collapse/lv-005.json` → `Assets/Prototype/Resources/Levels/lv-005.json`
- Move: `docs/levels-collapse/lv-006.json` → `Assets/Prototype/Resources/Levels/lv-006.json`
- Modify: field `note` trong cả hai file

**Interfaces:**
- Consumes: toàn bộ luật Task 1-4.
- Produces: 6 level trong `Resources/Levels/`, tất cả qua solver cả hai chế độ — đây là cổng xuất bản.

- [ ] **Step 1: Chuyển file + sửa note**

```bash
cd "D:/CategorySort" && mv docs/levels-collapse/lv-005.json Assets/Prototype/Resources/Levels/lv-005.json && mv docs/levels-collapse/lv-006.json Assets/Prototype/Resources/Levels/lv-006.json && rmdir docs/levels-collapse
```

Trong mỗi file, thay cả câu `note` bằng bản ngắn (Edit từng file):
- lv-005: `"Chuyen tu BUILTIN[1] cua demo wordstack.html. COLLAPSE 1 tang: hospital/school/airport -> building."`
- lv-006: `"Chuyen tu BUILTIN[2] cua demo wordstack.html. COLLAPSE 2 tang: dog/cat/bird -> pet -> hobby. Nhom dog co art rieng: tile 'Cho' sinh ra mang hinh con cho."`

- [ ] **Step 2: Chạy cổng**

```bash
cd "D:/CategorySort" && ./selfcheck.sh
```

Expected: `Level: lv-001..lv-006` và `SelfCheck OK — 6 level, ...` với số nước của lv-005/lv-006 in ra ở cả chặt lẫn rộng.

**Nếu FAIL ở lv-005/lv-006:** KHÔNG tự đổi luật, KHÔNG tự đổi bố cục level. Ghi lại nguyên văn `r.Why` + chế độ nào fail, thử một lần với beam lớn hơn để phân biệt "không giải được" với "beam hụt" (sửa tạm `Solver.Solve(..., beam: 2000)` trong SelfCheck, chạy, rồi HOÀN TÁC), và báo user với số liệu. Đây là điểm dừng chờ quyết định.

- [ ] **Step 3: Cho Unity import + commit kèm .meta**

Focus cửa sổ Unity (repo chính đang mở), đợi nó import 2 file mới, kiểm tra 2 file `.meta` xuất hiện:

```bash
cd "D:/CategorySort" && ls Assets/Prototype/Resources/Levels/lv-005.json.meta Assets/Prototype/Resources/Levels/lv-006.json.meta
```

Expected: cả hai tồn tại. (Bỏ qua bước này nếu Unity đóng — nhưng khi đó PHẢI mở lại trước commit; thiếu .meta là mỗi máy tự sinh GUID, dự án đã dính một lần với BoardTestDriver.)

```bash
cd "D:/CategorySort" && git add Assets/Prototype/Resources/Levels/lv-005.json Assets/Prototype/Resources/Levels/lv-005.json.meta Assets/Prototype/Resources/Levels/lv-006.json Assets/Prototype/Resources/Levels/lv-006.json.meta && git commit -m "$(cat <<'EOF'
Ship the two collapse levels from the reference demo

lv-005 is one tier (three groups feeding building), lv-006 chains two
tiers and gives the dog group its own art. Both pass the solver gate in
both box-removal modes.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

(File gốc trong `docs/levels-collapse/` chưa từng được commit nên `mv` + add đích mới là đủ, không cần `git rm`.)

---

### Task 6: View — animation collapse + sổ sách

**Files:**
- Modify: `Assets/Prototype/BoardController.cs:416-445` (coroutine `Settle`), thêm hàm mới cạnh `SpawnTiles` (`:489`)
- Modify: `design/gdd/systems-index.md` (dòng COLLAPSE ở Enumeration + Progress Tracker)
- Modify: `production/session-state/active.md` (mục "Đang ở đâu")

**Interfaces:**
- Consumes: `SettleKind.Collapse`, `SettleEvent.NewTileUid` (Task 3).
- Produces: người chơi thấy 4 thẻ co về 0 rồi thẻ mới nở ra tại chỗ (pop OutBack), màu hộp tính lại. Không có API mới cho ai tiêu thụ.

- [ ] **Step 1: Sửa coroutine `Settle`**

Trong vòng `for (;;)`, sau khối `if (ev.Kind == SettleKind.Clear) {...}`, thêm:

```csharp
                if (ev.Kind == SettleKind.Collapse)
                {
                    var seq = RemoveTiles(ev.DoomedUids);   // 4 thẻ co về 0 như CLEAR
                    if (seq != null) yield return seq.WaitForCompletion();
                    SpawnCollapsedTile(ev.Stack, ev.NewTileUid);
                    RefreshColors(ev.Stack);
                }
```

- [ ] **Step 2: Thêm `SpawnCollapsedTile` (đặt ngay dưới `SpawnTiles`)**

```csharp
        // Thẻ sinh ra từ collapse: dựng như SpawnTiles nhưng một thẻ, nở từ 0 để đọc ra
        // "4 thẻ vừa gộp thành cái này" thay vì "thẻ mới rơi xuống".
        void SpawnCollapsedTile(int s, string uid)
        {
            var box = g.TopBox(s);
            if (box == null) return;
            int i = Array.FindIndex(box.Slots, t => t != null && t.Uid == uid);
            if (i < 0) return;
            var t = box.Slots[i];
            var colors = Game.BoxColorIndices(box);
            var tv = Instantiate(tilePrefab, boxViews[s].Slot(i), false);
            tv.transform.localPosition = Vector3.zero;
            tv.Bind(t, ArtOf(t), ColorOf(colors, t.Uid), TileOrder, SlotSize);
            tv.transform.localScale = Vector3.zero;
            tv.transform.DOScale(1f, 0.18f).SetEase(Ease.OutBack).SetLink(tv.gameObject);
            tiles[t.Uid] = tv;
        }
```

(`using System;` đã có ở đầu file cho `Array.FindIndex` — kiểm, nếu chưa thì dùng `System.Array.FindIndex`.)

- [ ] **Step 3: Compile cả hai assembly**

```bash
cd "D:/CategorySort" && ./compilecheck.sh
```

Expected: `game.dll OK` + `editor.dll OK` (chỉ warning CS0649 quen thuộc của SerializeField).

- [ ] **Step 4: Cập nhật sổ sách**

`design/gdd/systems-index.md` — normalize LF trước nếu Edit trượt vì CRLF:
- Bảng System Enumeration, dòng 19 COLLAPSE: cột Code `❌` → `⚠️`, description sửa `validate đang **chặn**` → `validate đã mở (2026-08-06)`.
- Progress Tracker, dòng COLLAPSE: Status `Not Started` → `In Progress`, GDD Path → `docs/superpowers/specs/2026-08-06-collapse-design.md`, Last Updated `2026-08-06`.

`production/session-state/active.md` — thêm vào mục "Đang ở đâu" một đoạn:

```markdown
**COLLAPSE đã land trong domain** (2026-08-06): nhóm có cha gộp thành 1 thẻ của nhóm cha,
chiếm chỗ trong chính hộp đó, không lộ hộp dưới. Chặt xoá hộp rỗng có `HadCollapse`.
Spec: `docs/superpowers/specs/2026-08-06-collapse-design.md`. Level: lv-005 (1 tầng),
lv-006 (2 tầng) đã qua solver cả hai chế độ. **Chưa ai kéo tay** animation collapse
(`SpawnCollapsedTile`) — nghiệm thu cùng đợt kéo thử bàn prefab.
```

- [ ] **Step 5: Nghiệm thu trong Editor (thủ công — không tự động hoá được input)**

Focus Unity → đợi compile xong (Console sạch lỗi) → Play `Assets/Scenes/Main.unity` → chuyển tới lv-005 (phím tắt chuyển level xem HUD/help của bản chạy; nếu không có, tạm sửa `levelIndex` khởi đầu rồi hoàn tác) → kéo cho đủ 4 thẻ một nhóm con vào một hộp → PHẢI thấy: 4 thẻ co biến, 1 thẻ mới nở ra tại chỗ, hộp KHÔNG biến mất, HUD đếm tăng 1. Kéo thẻ mới đi → hộp rỗng lùi ra (đang chạy luật rộng). Nếu Unity không mở được ngay, ghi rõ trạng thái "chưa nghiệm thu tay" — KHÔNG được báo hoàn tất mục này.

- [ ] **Step 6: Commit**

```bash
cd "D:/CategorySort" && git add Assets/Prototype/BoardController.cs design/gdd/systems-index.md production/session-state/active.md && git commit -m "$(cat <<'EOF'
Play the collapse on screen: four tiles shrink, one blooms in place

The view handles SettleKind.Collapse with the same RemoveTiles beat as
CLEAR, then pops the new tile from scale zero in its slot. Bookkeeping
updated in systems-index and the session handoff note.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Ngoài phạm vi plan này (đã ghi trong spec Mục 9)

- `Score` heuristic của solver — chỉ đụng nếu Task 5 cho thấy beam hụt, và đụng theo số liệu.
- Undo, `BoxCapacity > GroupSize`, animation cầu kỳ hơn cho collapse.
- Công thức % tiến trình của bộ sinh tile động (spec Mục 7) — sửa khi tính năng đó quay lại.
